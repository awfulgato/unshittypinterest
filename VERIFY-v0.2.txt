Eskja v0.2 verification manifest

BASELINE: v0.1 remains the known-good baseline.

CURRENT MEDIA PASS:
  app.js — existing image/audio/video upload and renderer path
  board.html — + accepts image/audio/video and loads functional media fixes
  styles.css — existing cursor/media/cutout skin
  media-functional.css — temporary functional fixes for media selection controls
  setup.sql — fresh installs admit image/note/audio/video and cutout fields
  migrations/2026-08-21-media-things.sql — existing Supabase projects: run this once

IMPORTANT:
  The live Supabase project must receive migrations/2026-08-21-media-things.sql
  before audio/video rows can be inserted. The old database constraint only
  admits image/note even though the front end already knows how to render
  audio/video.

FUNCTIONAL MEDIA GATE BEFORE THE AESTHETIC PASS:
  - image/GIF upload, render, move, resize, delete, reload
  - MP3 upload, play/pause, move, resize, delete, reload
  - WAV upload, play/pause, move, resize, delete, reload
  - browser-playable video upload, play/pause/seek, move, resize, delete, reload
  - one audible thing plays at a time
  - failed/unsupported playback remains visible as a failure rather than
    silently changing the stored thing

AUDIO/VIDEO AESTHETICS COME AFTER THIS GATE PASSES.
